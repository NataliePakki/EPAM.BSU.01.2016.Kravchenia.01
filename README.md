EPAM.BSU.01.2016.Kusok.01
«адание 1.

Х –азработать метод, позвол€ющий вычисл€ть корень n-ой степени из числа методом Ќьютона с заданной точностью.

Х ѕроверить работу метода (сравненить полученный результат со значением, рассчитываемым с помощью метода Math.Pow библиотеки классов .NET Framework), использу€ консольное приложение с интерфейсом командной строки.

«адание 2.

ƒан непр€моугольный целочисленный массив (jagged array). –еализовать метод "пузырьковой" сортировки (метод класса Array не использовать!), таким образом, чтобы была возможность упор€дочить строки матрицы, в частности,

Х в пор€дке возрастани€(убывани€) сумм элементов строк матрицы;

Х в пор€дке возрастани€(убывани€) максимальных элементов строк матрицы;

Х в пор€дке возрастани€(убывани€) минимальных элементов строк матрицы.